#pragma once
#include <string>

// Encodes a payload with the OFDM modem (Encoder48k) and reports the outcome
std::string modem73_loopback_selftest();

// bjuild line
std::string modem73_build_info();
